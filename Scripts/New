let
    Import =
        let
            e1 = #table(
                type table [id = Int16.Type, regiao = text],
                {{1, "Norte"}, {2, "Nordeste"}, {3, "Centro-Oeste"}, {4, "Sudeste"}, {5, "Sul"}, {6, "Outros"}}
            )
        in
            e1
in
    Import
